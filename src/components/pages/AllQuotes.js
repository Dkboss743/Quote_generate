import QuoteList from "../quotes/QuoteList";
import Card from "../UI/Card";

const AllQuotes = () => {
  return (
    <Card>
      <QuoteList></QuoteList>
    </Card>
  );
};

export default AllQuotes;
